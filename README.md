# Randomized Kaczmarz Visualizer

Interactive React demo for explaining randomized Kaczmarz projections in 2D and 3D.

## Run locally

```bash
npm install
npm run dev
```

Then open the local URL printed by Vite, usually `http://localhost:5173/`.

## Build locally

```bash
npm run build
npm run preview
```
